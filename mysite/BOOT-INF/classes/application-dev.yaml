server:
  port: 8080  # 开发使用的端口号

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

# 配置日志
logging:
  level:
    root: info
    com.zzw.mysite: debug
  file:
    path: log/blog-dev.log






